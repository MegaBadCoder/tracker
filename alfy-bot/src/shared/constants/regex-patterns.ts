export const DATE_PATTERNS = {
  DMY: /^(\d{1,2})\.(\d{1,2})\.(\d{4})$/,
  DAYS: /через\s+(\d+)\s+(день|дня|дней)/,
  WEEKS: /через\s+(\d+)\s+(неделю|недели|недель)/,
  MONTHS: /через\s+(\d+)\s+(месяц|месяца|месяцев)/,
} as const;
